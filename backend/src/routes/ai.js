import {Router} from 'express';import {auth} from '../middleware/auth.js';import {chat,nextAction,analyzeGoal,weeklyPlan,progressInsight} from '../controllers/aiController.js';const r=Router();r.use(auth);r.post('/chat',chat);r.post('/next-action',nextAction);r.post('/weekly-plan',weeklyPlan);r.post('/progress-insight',progressInsight);r.post('/analyze-goal',analyzeGoal);export default r;
